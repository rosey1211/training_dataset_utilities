This module will take in multiple datasets and create a "training" and "test" dataset for machine learning.  The algorithm will take a sample of images from each of the specified original datasets and add a percentage to the training and test data.  In addition, the labels.csv files will be adjusted for both the training and test data.  All of the settings for this module are in the dataset_manager.ini file.

Usage:
>  ./dataset_manager